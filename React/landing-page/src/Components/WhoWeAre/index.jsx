import { Container } from "react-bootstrap";

export default function WhoWeAre() {
    return (
        <Container fluid={true} className={`wrapper-container`}>
            
        </Container>
    );
}